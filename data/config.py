   # Central location for all tunable numbers.
   # Things like scan times, delay ranges, penalty values,
   # spawn weights, contrast wait time, staffing arc.
   # Changing a number here affects the whole simulation.
   # No logic lives here — just values.
