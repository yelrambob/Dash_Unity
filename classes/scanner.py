      # Scanner class — id, zone (ED or Main),
      # is_busy flag, current patient,
      # scan timer, cooldown timer,
      # assigned tech, availability flag.
      # States: Idle, Scanning, Cooldown, Locked (no tech)
