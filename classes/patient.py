      # Patient class — id, acuity, personability,
      # mobility type, exam list, transport object,
      # current state, wait timer, contrast timer.
      # Full state list:
      #   Ordered > InTransport > InHolding >
      #   ContrastOrdered > ContrastReady >
      #   InjectorReady > Scanning > Cooldown >
      #   Leaving > Completed  Refused  Holdover
   
